# Example environment variables for local prototype
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
DATABASE_URL=./db/eatom.db
NUXT_PUBLIC_APP_URL=http://localhost:3000
# Replace with a secure secret in real deployments. DO NOT commit real secrets.
BETTER_AUTH_SECRET=replace_this_with_a_secure_random_value
# SQLite database file path
DATABASE_URL=./db/eatom.db

# App base URL (used by better-auth for trusted origins)
NUXT_PUBLIC_APP_URL=http://localhost:3000

# better-auth secret (generate with: openssl rand -hex 32)
BETTER_AUTH_SECRET=change-me-generate-a-strong-secret
